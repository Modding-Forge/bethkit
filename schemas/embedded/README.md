# Embedded schema assets

This directory receives manually reviewed schema release artifacts from the
separate xDump workspace.

Do not copy candidate exports here. Commit `bethkit.bkschemas`, the eleven
matching `.bkschema` packages, and their reviewed checksums only after every
xDump release gate passes.
